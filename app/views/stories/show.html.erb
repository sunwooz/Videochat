<p>
	<%= link_to edit_course_story_path(@story), :class => 'btn btn-info' do %>
		<i class="icon-white icon-pencil"></i> Edit
	<% end %>
	<%= link_to course_story_path(@story), :method => :delete, :class => 'btn btn-danger' do %>
		<i class="icon-white icon-trash"></i> Destroy
	<% end %>
<p>
	<%= id_to_language(@course.language_id) %>
</p>
<p>
	<%= id_to_category(@story.category_id) %>
</p>
<p>
	<%= link_to @course.name, @course %>
</p>
<p>
	<%= link_to '/conversation', :class => 'btn btn-success' do %>
		<i class="icon-ok icon-white"></i> StoryChat!
	<% end %>
</p>
<p>
	<%= @story.name %>
</p>
<h1>Lesson</h1>
<p>
	<%= markdown(@story.lesson) %>
</p>
