<p>
	<%= link_to 'edit', edit_course_story_path(@story), :class => 'btn btn-info' %>
	<%= link_to 'destroy', course_story_path(@story), :method => :delete, :class => 'btn btn-danger' %>
<p>
	<%= id_to_language(@course.language_id) %>
</p>
<p>
	<%= link_to @course.name, @course %>
</p>
<p>
	<%= link_to '/conversation', :class => 'btn btn-success' do %>
		<i class="icon-ok icon-white"></i> StoryChat!
	<% end %>
</p>
<p>
	<%= @story.name %>
</p>
<h1>Lesson</h1>
<p>
	<%= markdown(@story.lesson) %>
</p>
