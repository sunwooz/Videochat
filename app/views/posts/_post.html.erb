<% @posts.each do |post| %>
    <h1 id="postTitle"><%= link_to post.title, post_path(post) %></h1>
	<p>Posted On: <%= post.created_at %></p>
    <p id="postBody">
		<%= markdown(post.body) %>
	</p>
		<%= link_to "Edit", edit_post_path(post), :class => "btn" %>
		<%= link_to "Destroy", post_path(post), :method => :delete, :class => :destroy %>
	<hr/>
<% end %>