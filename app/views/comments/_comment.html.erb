<%= div_for comment do %>
	<div id="commentbox">
	<h2><%= comment.name %></h2> <h4>Posted On: <%= comment.created_at %></h4>
	<p>
		<%= markdown(comment.body) %>
	</p>
	</div>
	<p>
		<% if admin? %>
					<%= link_to "Edit", edit_post_comment_path(comment) %>
					<%= link_to "Destroy", post_comment_path(comment), :method => :delete, :class => :destroy %>
		<% end %>
	</p>
<% end %>